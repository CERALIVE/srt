name: Windows

on:
  push:
    branches: [ "master", "dev" ]
  pull_request:
    branches: [ "master", "dev" ]

jobs:
  build:

    name:  C++11 noenc
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v7
    - name: Configure ccache environment
      run: |
        $cacheDir = Join-Path $env:RUNNER_TEMP 'ccache'
        "CCACHE_DIR=$cacheDir" >> $env:GITHUB_ENV
    - name: Restore ccache
      uses: actions/cache/restore@v6
      with:
        path: ${{ runner.temp }}/ccache
        key: srt-ccache-cxx11-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('CMakeLists.txt', 'cmake_object_lib_support.c', 'apps/**', 'common/**', 'haicrypt/**', 'scripts/**', 'srtcore/**', 'test/**', 'testing/**') }}
        restore-keys: srt-ccache-cxx11-${{ runner.os }}-${{ runner.arch }}-
    - name: Set up ccache
      run: |
        choco install ccache ninja -y --no-progress
        ccache --max-size=200M
        ccache --zero-stats
    - name: Set up MSVC environment
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x64
    - name: configure
      run: |
        md _build && cd _build
        cmake ../ -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache -DENABLE_STDCXX_SYNC=ON -DENABLE_ENCRYPTION=OFF -DENABLE_UNITTESTS=ON -DENABLE_BONDING=ON -DENABLE_LOCALIF_WIN32=ON -DUSE_CXX_STD=c++11
    - name: build
      run: cd _build && cmake --build ./ --config Release --verbose
    - name: test
      run: cd _build && ctest -E "TestIPv6.v6_calls_v4|TestConnectionTimeout.BlockingLoop" --extra-verbose -C Release
    - name: Save ccache
      if: ${{ success() && github.event_name != 'pull_request' }}
      uses: actions/cache/save@v6
      with:
        path: ${{ runner.temp }}/ccache
        key: srt-ccache-cxx11-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('CMakeLists.txt', 'cmake_object_lib_support.c', 'apps/**', 'common/**', 'haicrypt/**', 'scripts/**', 'srtcore/**', 'test/**', 'testing/**') }}
    - name: Show ccache statistics
      if: always()
      run: ccache --show-stats
